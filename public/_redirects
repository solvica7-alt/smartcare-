# Netlify Redirects Rule
# 🇵🇸 RULES ARE PROCESSED TOP-TO-BOTTOM. SPECIFIC RULES FIRST!

# 1. Proxy JSONBlob requests (Must be before SPA rule)
/api/sync/*  https://jsonblob.com/api/jsonBlob/:splat  200!

# 2. SPA Catch-all (Must be last)
/*    /index.html   200
